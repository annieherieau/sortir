{#  Route en cours : pour rendre le lien actif #}
{% set currentPath = app.request.attributes.get('_route') %}

{# les Administrateurs ont une navbar différente #}
<nav class="navbar navbar-expand-lg {{ is_granted('ROLE_ADMIN') ? 'bg-sortir-adm' : 'bg-sortir'}}" data-bs-theme="dark">
    <div class="container">
        <div class="logo_container h2">
            <a class="navbar-brand" href="{{ url('sortie_index') }}">
                <img class="mr-3 mt-2" src="{{ asset('img/Sortir logo trasnp.png') }}" alt="logo" height="100">
                </a>
        </div>

        <div>
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ currentPath == 'sortie_index' ? 'active' : '' }}" href="{{ path('sortie_index') }}"
                    title="Accueil">
                    Accueil</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="#"
                           title="Administration">
                            Administration
                        </a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link {{ currentPath == 'sortie_create' ? 'active' : '' }}" href="{{ path('sortie_create') }}"
                           title="Créer une sortie">
                            Créer une sortie
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ currentPath == 'participant_myprofile' ? 'active' : '' }}" href="{{ path('participant_myprofile') }}"
                           title="Gérer mon profil">
                            Mon profil
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}"
                           title="Logout">
                            Logout
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link {{ currentPath == 'app_login' ? 'active' : '' }}"  href="{{ path('app_login') }}"
                           title="Login">
                            Login
                        </a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>